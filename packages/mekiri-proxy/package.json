{
  "name": "mekiri-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:live": "MEKIRI_PROXY_LIVE_TEST=1 vitest run test/live.smoke.test.ts",
    "build": "tsc -p tsconfig.json && npm run typecheck:hook",
    "typecheck:hook": "tsc -p tsconfig.hook.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "mekiri-core": "file:../mekiri-core",
    "tsx": "^4.19.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
