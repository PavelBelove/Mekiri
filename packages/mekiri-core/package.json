{
  "name": "mekiri-core",
  "version": "0.1.0",
  "description": "Pure primitives for prune/sprout: transcript slicing, quote-based boundary addressing, fruit schemas.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^4.0.0",
    "@anthropic-ai/claude-agent-sdk": "^0.3.218"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
