# Claude Code local/machine-specific settings (per-developer, not shared)
.claude/settings.local.json
.claude/*.local.json

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Scratch/tmp
*.tmp

# Node / TypeScript build artifacts
node_modules/
dist/
*.tsbuildinfo

# subagent-driven-development ledger/scratch (per superpowers:subagent-driven-development)
.superpowers/

# mekiri-host runtime state (audit log, config) created by actually using prune/sprout
.mekiri/

# git worktrees (per superpowers:using-git-worktrees)
.worktrees/
